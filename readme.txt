This project is called SEP.
This project is built using C++ language.
It includes 9 files: login.h, login.cpp, request.h, request.cpp, employee.h, employee_record.h, service_production.h, service_production.cpp and ui.cpp
All codes' copyright reserved by Zekun Du
The programmer debugs the project on Windows by using Code::Blocks
So a Code::Blocks is enough for compile and run
Also LinuxOS Terminal has the same functionality, so does Visual Studio and other applications.
